{
    "name": "myvendor/myproject",
    "description": "My project",
    "require": {
        "php": "^7.4",
        "ext-json": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "platform": {
            "php": "7.4"
        }
    },
    "extra": {
        "github": {
            "actions": {
                "check-docker": {
                    "runs": {
                        "using": "composite",
                        "steps": [
                            {
                                "name": "Check Docker",
                                "run": "docker --version"
                            }
                        ]
                    }
                }
            }
        }
    }
}
